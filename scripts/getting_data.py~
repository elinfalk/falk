import requests
import untangle

doc = untangle.parse("http://www.grandcentral.org/developers/data/nyct/nyct_ene.xml") 
outages = doc.NYCOutages

noOfElements = len(outages)
i=0

print outages
#while i < noOfElements:
#	print outages[i].station
#	if outages[i].reason == "Repair":
#		print outages[i].reason
#	i+=1

print noOfElements
